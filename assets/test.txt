$(eval
  const u = "$(user)", channel = "TieuLinh Hota", rand = Math.random(), gold = 10000, r = $(urlfetch json http://tieulinhhota.com/assets/random.json);
  const getM = (list) => list[Math.floor(Math.random() * list.length)].replace("{user}", u).replace("{channel}", channel).replace("{gold}", gold).replace("{emoji}", r.emojis[Math.floor(Math.random() * r.emojis.length)]);
  if (rand > 0.95) getM(r.high); 
  else if (rand < 0.5) getM(r.medium); 
  else getM(r.low);
)
